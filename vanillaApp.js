//creating objects to target HTML
const formID = document.getElementById('formID');
const listID = document.getElementById('listID');

//creating function to add tiles when submit button is pushed

function submitButton(e){
    e.preventDefault();
    const itemText = ()
    
}

//I need to create a function that posts the input text from the form and write's it in the </ul>

function placeMarker()

//I'm declaring the variable "postTodo" which will equal what the user puts in the form's text.input (html id = postTodo)

const postTodo = document.getElementById('postTodo');

//creating write function to post 

document.getElementById("listID").innerHTML = postTodo;
